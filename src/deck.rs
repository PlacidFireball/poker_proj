use crate::card::Card;

#[allow(dead_code, unused_variables)]
#[derive(Clone, Debug)]
struct Deck {
    cards: Vec<Card>
}
#[allow(dead_code, unused_variables)]
impl Deck {
    fn init(mut self) -> Deck {
        unimplemented!()
    }
}